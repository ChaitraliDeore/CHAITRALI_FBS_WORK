#include<stdio.h>
void main()
{
    float b,h,area;
    printf("Enter the base & height of triangle:");
    scanf("%f %f",&b,&h);
    area=(b*h)/2;
    printf("Area of triangle is %f",area);

}